import 'package:flutter/material.dart';


class AcceptHomeScreen extends StatelessWidget {

  const AcceptHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('수락 확인 스크린'),
      ),
    );
  }
}
