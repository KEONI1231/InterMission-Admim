import 'package:flutter/material.dart';



class ResearchHomeScreen extends StatelessWidget {

  const ResearchHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Center(
            child: Text('리서치 등록 확인'),
          ),

        ],
      ),

    );
  }
}
